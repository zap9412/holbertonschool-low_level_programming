#include "main.h"

/**
 * _abs - compoutes the absolute value of an integer
 * @n: the integer to check
 *
 * Return: the absolute of n
 */
 int _abs(int n)
 {
    if (n < 0)
    {
        return (-n);
    }
    return (n);
 }
 